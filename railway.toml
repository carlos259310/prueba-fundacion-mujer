[build]
builder = "nixpacks"

[build.nixpacksPlan.phases.setup]
nixPkgs = ["dotnet-sdk_8"]

[deploy]
startCommand = "dotnet ProductCatalog.Api.dll"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
